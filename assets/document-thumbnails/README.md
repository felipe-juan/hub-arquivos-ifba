# Miniaturas de documentos

Este diretório é preenchido automaticamente por:

```bash
python3 scripts/generate_documents_manifest.py
```

Para PDFs, o gerador cria WebP responsivos de 160, 320 e 520 px quando `PyMuPDF` e `Pillow` estão instalados. Não edite os arquivos gerados manualmente.
